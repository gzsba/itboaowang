# IT宝网内容更新规则

本文件用于规范后续每次更新流程。  
执行任何更新前，先读取本文件，再按规则操作。

## 1. 更新目标

- 保持页面风格稳定：资讯优先、简洁美观、信息密度适中。
- 仅更新新闻内容，不随意改动页面结构与视觉体系。
- 新闻以”北京时间最近 12 小时”窗口为准，强调时效性与可信来源。超过 12 小时的新闻一律不采用。

## 2. 文件与变更边界

- 主要操作文件：`index.html`
- 默认只改以下区域：
  - `AI热点新闻`模块内的新闻卡片（标题、摘要、来源、链接）
- 默认不改以下内容：
  - 页面主视觉风格（配色、间距、卡片体系）
  - 导航结构、工具区、OpenClaw专题、页脚文案
  - 新闻区标题右侧说明文字（当前为：`资讯优先 · 精简阅读`）

如需改“非新闻区”内容，必须明确说明并单独确认。

## 3. 新闻筛选规则

### 3.1 时间范围

- **强制限制：只采集”北京时间最近 12 小时”内新闻。** 发布时间距今超过 12 小时的一律不采用。
- 若 12 小时内高质量新闻不足，宁可减少条数并标注”当前时段高质量新闻较少”。

### 3.2 主题范围

- 大模型发布与能力升级
- AI 基础设施/芯片/算力平台
- Agent 与企业 AI 落地
- 机器人与具身智能
- 行业政策/标准（仅限有明确官方来源）

### 3.3 来源质量

优先来源（按优先级）：

1. 官方发布（公司官网、官方博客、官方 newsroom）
2. 主流媒体与权威科技媒体
3. 二级聚合媒体（仅在找不到更高质量来源时使用）

### 3.4 内容质量

- 不使用纯标题党、无正文支撑或明显营销稿。
- 避免重复报道同一事件（同一事件保留 1 条最佳来源）。
- 每条摘要控制在 1-2 句，强调”发生了什么 + 为什么重要”。

### 3.5 链接可访问性（强制）

- **每条新闻的链接必须可正常访问且能看到原文内容。**
- 禁止使用以下链接：付费墙链接、需登录才能查看的链接、404/死链、聚合器跳转页（无法看到正文）、App深链（如`xhs://`、`weixin://`）。
- 优先使用公开可访问的网页链接（`https://`开头）。
- **必须用 WebFetch 工具逐条验证每个链接可正常抓取到正文内容**，无法抓取到正文的链接一律更换或放弃该条新闻。

## 4. 页面更新格式（新闻卡片）

每条新闻卡片应包含：

- 标题：建议前缀日期（如 `03-24｜...`），突出时效
- 摘要：不超过 90-120 字
- 来源：简洁标注媒体/机构名
- 链接：优先原始报道或官方页，**必须可访问且能看到原文正文（用 WebFetch 验证）**

建议保留 6 条新闻卡片，保证首页可读性。

## 5. 标准更新流程

1. 读取本文件 `UPDATE_RULES.md`
2. **抓取北京时间最近 12 小时内新闻（中英文可混合）**，超过 12 小时一律不采用
3. 按规则筛选并去重
4. 仅替换 `index.html` 新闻区卡片内容
5. 做可渲染性检查（HTML parse）
6. 快速人工复核：
   - **链接是否有效（必须用 WebFetch 逐条验证能抓取到正文）**
   - 日期是否合理
   - 摘要是否通顺、无夸张表述
7. 根据需要执行 git 提交与推送

## 6. 校验清单（每次更新必查）

- [ ] 新闻区标题文字未被改动
- [ ] 非新闻区结构未被误改
- [ ] 新闻条数为 6（或明确说明调整）
- [ ] 每条都有来源与链接
- [ ] **每个链接已用 WebFetch 验证可访问且能看到正文**
- [ ] 无明显乱码、坏标签、断裂链接
- [ ] 页面解析正常（HTML parse OK）

## 7. 提交规范（如需提交）

推荐提交信息：

- `update AI news YYYY-MM-DD`

如包含结构修复：

- `update AI news YYYY-MM-DD and fix markup`

## 8. 异常处理

- 若检索结果质量不足：宁缺毋滥，减少条数并标注“今日高质量新闻较少”。
- 若发现页面有意外文件变更：先暂停并确认，不自动提交无关改动。
- 若链接来源冲突：优先官方来源，无法确认则不收录。
